import * as Styled from '@/components/order/SectionHeading/SectionHeading.styled';

const SectionHeading = ({ children }) => <Styled.SectionHeading>{children}</Styled.SectionHeading>;

export default SectionHeading;
